// Circunference Area = pi * rˆ2

const PI = Math.PI;
let radius = 2;

const circunferenceArea = (PI * Math.pow(radius, 2));
console.log('Circunference area is: ' + circunferenceArea.toPrecision(4) + 'm2');