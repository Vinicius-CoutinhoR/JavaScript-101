// Avoid generic types and acronyms
let any = 'Hello!';
console.log('Any variable which ' + any + ' is a: ', typeof any);

any = 3.512;
console.log('Any variable which ' + any + ' is a: ', typeof any);