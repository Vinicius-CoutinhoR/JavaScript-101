let number = 1;
{
    let number = 2;
    console.log('Inside:', number);
}
console.log('Outside:', number);

// Var variables have only two scopes:
// Global scope and function scope

// On the other hand let variables have three scopes
// Global, Block and function scope.
